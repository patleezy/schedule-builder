import React from 'react'
import ReactDOM from 'react-dom/client'
import WorkbackBuilder from './WorkbackBuilder'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WorkbackBuilder />
  </React.StrictMode>
)
